"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const axios_1 = __importDefault(require("axios"));
/**
 * Fetches all links from a sitemap URL, including nested sitemaps.
 * @param {string} url - URL of the sitemap to fetch links from.
 * @param {IOptions} [options] - Optional configuration options for the function.
 * @returns {Promise<string[]>} - A promise that resolves with an array of all links found in the sitemap(s).
 */
const GetSitemapLinks = (url, options = {}) => __awaiter(void 0, void 0, void 0, function* () {
    const regex = /(?<=<loc>)(.+?)(?=<\/loc>)/g;
    let indexes = [];
    let links = [];
    try {
        const { data } = yield axios_1.default.get(url);
        indexes = [...new Set(data.match(regex))];
    }
    catch (err) {
        return [];
    }
    if (indexes.length === 0 || !indexes[0].endsWith(".xml")) {
        return indexes;
    }
    const { filterIndexes } = options;
    if (filterIndexes) {
        indexes = indexes.filter((e) => e.includes(filterIndexes));
    }
    yield Promise.allSettled(indexes.map((index) => __awaiter(void 0, void 0, void 0, function* () {
        try {
            const { data: indexData } = yield axios_1.default.get(index);
            const array = indexData.match(regex);
            if (array)
                links.push(...array);
        }
        catch (err) { }
    })));
    return [...new Set(links)];
});
exports.default = GetSitemapLinks;
